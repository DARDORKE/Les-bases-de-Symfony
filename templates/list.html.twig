<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{{ title }}</title>
</head>
<body>
<h1>{{ title|upper }}</h1>
<p>{{ content }}</p>
<h2>Le dernier article</h2>
<p>{{ last_post.title }} écrit le {{ last_post.createdAt|date('d/m/Y') }}</p>
</body>
</html>